# Hermes · H-L1 rollover 日历同步

> **CANCELLED (2026-06-08)**：superseded by `remove-feishu-learning-calendar`。下列任务 **作废**，勿 apply。

## 1. 实现（作废）

- ~~1.1 `cmd_rollover`：patch 飞书日历~~
- ~~1.2 lark-cli patch/verify~~
- ~~1.3 `calendar_errors[]`~~

## 2. 现行验收

- `rollover` 后 JSON `scheduled_date` 正确即可（无日历字段）
