## Tasks

- [x] 1. Add failing regression tests for break-run display mode and dedicated GIF asset mapping.
- [x] 2. Update the pet renderer and break-run display entry to use `.breakRunning` with `LineDog/breakRunning` assets.
- [x] 3. Run the focused test suite and mark this change complete when green.
